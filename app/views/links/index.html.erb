<h1>Links</h1>
<% if flash[:notice] %>
  <p><%= flash[:notice] %></p>
<% end %>

<%= turbo_stream_from :links %>

<ul id="links">
  <%= render partial: "links/link", collection: @links, locals: { session_id: session.id.to_s } %>
</ul>

<%= link_to "New Link", new_link_path %>
